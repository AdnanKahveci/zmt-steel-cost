<UserControl x:Class="ZMT.SteelCost.App.Views.ProjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="0,0,10,20">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <StackPanel><TextBlock Text="{Binding Title}" FontSize="28" FontWeight="SemiBold" /><TextBlock Text="{Binding Subtitle}" Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,20" /></StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,14"><Button Content="＋ Yeni Proje" Style="{StaticResource PrimaryButton}" Command="{Binding Owner.NewProjectCommand}" Margin="0,0,8,0" /><Button Content="Yenile" Style="{StaticResource SecondaryButton}" Command="{Binding Owner.RefreshRecentCommand}" /></StackPanel>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Owner.RecentProjects}" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Firma" Binding="{Binding Company}" Width="2*" /><DataGridTextColumn Header="Müşteri" Binding="{Binding CustomerName}" Width="2*" />
                <DataGridTextColumn Header="CRM No" Binding="{Binding CrmNumber}" Width="*" /><DataGridTextColumn Header="Aşama" Binding="{Binding Stage, Converter={StaticResource EnumToTurkish}}" Width="2*" />
                <DataGridTextColumn Header="Tarih" Binding="{Binding DocumentDate, StringFormat=d}" Width="*" />
                <DataGridTemplateColumn Header="İşlem" Width="110"><DataGridTemplateColumn.CellTemplate><DataTemplate><Button Content="Projeyi Aç" Style="{StaticResource SecondaryButton}" Command="{Binding DataContext.Owner.OpenProjectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" /></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
